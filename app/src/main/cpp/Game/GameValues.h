#pragma once

inline bool some_feature = false;

// ESP configuration variables used in Menu & ESP
inline bool esp_enabled = false;

inline float clappedFloat            = 200.0f;  // max distance clamp (if you want to use it later)
inline float topPosFloat             = 2.0f;
inline float botPosFloat             = 0.0f;
inline float calculatedPositionFloat = 2.5f;
inline float bonesColor[4]           = {1.0f, 0.0f, 0.0f, 1.0f}; // red
